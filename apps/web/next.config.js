/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['picsum.photos', 'loremflickr.com', 'cdn.jsdelivr.net', 'avatars.githubusercontent.com'],
  },
};

module.exports = nextConfig; 